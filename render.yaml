services:
  - type: web
    name: wingo-predictor
    runtime: docker
    plan: free
    envVars:
      - key: HOST
        value: 0.0.0.0
      - key: PORT
        value: 8000
    disk:
      name: wingo-data
      mountPath: /app/data
      sizeGB: 1
